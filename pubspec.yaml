name: food_door
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^2.1.0
  provider: ^6.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.2
  flutter_lints: ^1.0.0

 #launcher icon 
flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/ic_launcher/web_hi_res_512.png"

flutter:
  uses-material-design: true
  # image assets
  assets:
    - assets/images/
    - assets/images/onboarding_images/
    - assets/images/ic_launcher/

